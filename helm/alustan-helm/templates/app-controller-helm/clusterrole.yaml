apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: app-manager
rules:
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["*"]

- nonResourceURLs: [""]
  verbs: ["*"]
